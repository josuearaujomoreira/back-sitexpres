3️⃣ Fazer rebuild da API sem tocar no banco

# Fazer build da api-sitexpres  somente 
docker compose build api
docker compose up -d api

#docker stop // rm 
 docker run -d   --name api-siteexpres-container   -p 3000:3000   --env-file .env   api-siteexpres


#cron vericar files
ls -lh ~/backups/postgres/
